[project]
name = "garden-of-the-forgotten-prompt"
version = "0.1.0"
description = "Google ADK agent project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-adk[extensions]",
    "python-dotenv",
    "requests",
]

[tool.uv]
package = false
